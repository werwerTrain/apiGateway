apiVersion: v1
kind: Service
metadata:
  name: apiGateway-service
spec:
  selector:
    app: apiGateway
  ports:
    - protocol: TCP
      port: 9092
      targetPort: 9092
      nodePort: 30012
  type: NodePort
